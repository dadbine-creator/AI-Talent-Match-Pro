# ── Secrets / env ──
.env
.env.*
*.env
!.env.example

# ── Database ──
*.db
*.sqlite
*.sqlite3

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
.python_packages/

# ── Build / deploy artifacts ──
*.zip
deployments/
LogFiles/
.azure/

# ── Node / frontend ──
node_modules/
talent-match-frontend/node_modules/
dist/
build/

# ── OS / editor ──
.DS_Store
.vscode/

# ── Key material (added during security review) ──
*.pem
*.key
*.p12
*.pfx
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
service-account*.json
*.publishsettings
.env.bak
secrets/

# ── Test / tooling caches ──
.pytest_cache/
.ruff_cache/
.mypy_cache/
htmlcov/
.coverage

# ── Nested project with its OWN git history ──
# talent-match-frontend/ is already an independent repo (2 commits).
# Committing it here would embed a gitlink and lose that history.
# Version it separately, or convert it to a submodule deliberately.
talent-match-frontend/

# ── Local scratch ──
....
